# Copy to .env and adjust. docker-compose reads these.
# Leave APP_PASSWORD empty to disable the login gate (e.g. trusted LAN only).
APP_PASSWORD=change-me
SESSION_SECRET=generate-with-openssl-rand-hex-32
